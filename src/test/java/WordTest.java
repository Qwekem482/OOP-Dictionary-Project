/*import org.junit.Test;

import static org.junit.Assert.assertSame;

public class WordTest {
    Word w1 = new Word("Hello", "Xin chao");
    Word w2 = new Word();

    @Test
    public void testGetWordTarget() {
        assertSame("Hello", w1.getWord_target());
    }

    @Test
    public void testGetWordExplain() {
        assertSame("Xin chao", w1.getWord_explain());
    }

    @Test
    public void testSetWordTarget() {
        w2.setWord_target("House");
        assertSame("House", w2.getWord_target());
    }

    @Test
    public void testSetWordExplain() {
        w2.setWord_explain("Ngoi nha");
        assertSame("Ngoi nha", w2.getWord_explain());
    }

    @Test
    public void testConstructor3() {
        Word w3 = new Word();
        assertSame("nothing", w3.getWord_target());
        assertSame("nothing", w3.getWord_explain());
    }

    @Test
    public void testConstructor2() {
        Word w4 = new Word(w1);
        assertSame("Hello", w4.getWord_target());
        assertSame("Xin chao", w4.getWord_explain());
    }

    @Test
    public void testConstructor1() {
        Word w5 = new Word("Hi", "Chao");
        assertSame("Hi", w5.getWord_target());
        assertSame("Chao", w5.getWord_explain());
    }
}
*/

